[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
cmds = ["yarn install --frozen-lockfile || yarn install"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn preview --host 0.0.0.0 --port ${PORT:-3000}"
